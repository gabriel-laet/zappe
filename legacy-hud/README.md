# Legacy wgpu HUD (archived)

The previous `cargo run` entry point (`winit` + `wgpu` CRT HUD) lives here for reference only.

The product UI is now **Tauri 2 + React** at the repository root (`npm run tauri dev`).
